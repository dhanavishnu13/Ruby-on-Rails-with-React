<h1>Categories</h1>

<table>
  <tr>
    
    <th>ID</th>
    <th>Name</th>
  </tr>

  <% @categories.each do |category| %>
    <tr>
      <td><%=  category.categoryId %></td>
      <td><%= category.name %></td>
    </tr>
  <% end %>
</table>

<%# <%= link_to "New category", new_category_path %>